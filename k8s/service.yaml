apiVersion: v1
kind: Service
metadata:
  name: inforganizer
  labels:
    app: inforganizer
spec:
  type: NodePort
  selector:
    app: inforganizer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
