---
###############################################################################
# Ansible playbook — configure nodes and deploy Inforganizer on k3s
#
# Usage (run from project root after terraform apply):
#   ansible-playbook -i ansible/inventory.ini ansible/install_k3s.yml
###############################################################################

- name: Common setup for all nodes
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Wait for SSH to become available
      wait_for_connection:
        timeout: 120

    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Install required packages
      apt:
        name:
          - curl
          - wget
          - git
          - apt-transport-https
          - ca-certificates
          - gnupg
          - lsb-release
        state: present

    - name: Disable swap (required by Kubernetes)
      command: swapoff -a
      changed_when: false

    - name: Remove swap entry from /etc/fstab
      lineinfile:
        path: /etc/fstab
        regexp: '^\s*[^#].*\sswap\s'
        state: absent

    - name: Install Docker
      apt:
        name: docker.io
        state: present

    - name: Enable and start Docker
      systemd:
        name: docker
        enabled: true
        state: started

    - name: Add ubuntu user to docker group
      user:
        name: ubuntu
        groups: docker
        append: true


- name: Install k3s on master node
  hosts: master
  become: true

  tasks:
    - name: Install k3s server
      shell: |
        curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="server --disable traefik" sh -
      args:
        creates: /usr/local/bin/k3s

    - name: Wait for k3s to be ready
      command: k3s kubectl get nodes
      retries: 10
      delay: 10
      register: result
      until: result.rc == 0

    - name: Fetch k3s node token
      slurp:
        src: /var/lib/rancher/k3s/server/node-token
      register: k3s_token_raw

    - name: Store master IP and token as facts
      set_fact:
        k3s_token: "{{ k3s_token_raw.content | b64decode | trim }}"
        master_ip:  "{{ ansible_host }}"


- name: Install k3s agent on worker nodes
  hosts: workers
  become: true

  tasks:
    - name: Install k3s agent and join cluster
      shell: |
        curl -sfL https://get.k3s.io | \
          K3S_URL=https://{{ hostvars[groups['master'][0]].ansible_host }}:6443 \
          K3S_TOKEN={{ hostvars[groups['master'][0]].k3s_token }} \
          sh -
      args:
        creates: /usr/local/bin/k3s


- name: Deploy Inforganizer to Kubernetes
  hosts: master
  become: true

  tasks:
    - name: Copy Kubernetes manifests to master
      copy:
        src: "{{ item }}"
        dest: /tmp/
      loop:
        - ../k8s/pvc.yaml
        - ../k8s/deployment.yaml
        - ../k8s/service.yaml

    - name: Apply PVC
      command: k3s kubectl apply -f /tmp/pvc.yaml

    - name: Apply Deployment
      command: k3s kubectl apply -f /tmp/deployment.yaml

    - name: Apply Service
      command: k3s kubectl apply -f /tmp/service.yaml

    - name: Wait for deployment to be ready
      command: k3s kubectl rollout status deployment/inforganizer --timeout=120s

    - name: Print app URL
      debug:
        msg: "Inforganizer is accessible at http://{{ ansible_host }}:30080"
